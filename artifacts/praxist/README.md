# Praxist artifacts（仓内指针）

**权威资产根目录不在本仓**，而在共享盘：

`/workspace/shared/praxist_assets/`

协议与恢复剧本见该目录 `README.md` / `RECOVERY.md`。

本目录仅作仓内导航；勿把大体积评测产物或 TimesFM 权重 commit 进 git。
归档由 `scripts/praxist_assets_archive.py` 在 harvest / slow_drain_complete 时自动写入 shared。
