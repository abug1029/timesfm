# 文档对齐当前代码与推进程度

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. This plan edits **documentation and comments only**. No production logic.

**Goal:** 让人类/Agent 文档与 2026-09-11 活代码、磁盘裁决、监督环快照一致；失效 spec 加横幅，不再像施工单。

**Architecture:** 按 neat-freak：合并优于追加。写清「代码现在怎样」，包括已知分叉（Copilot 日线主句、实盘日线裸读、`gate_pass` 不含 EV）。禁止把未改的代码写成已修好。

**Tech Stack:** WSL `/home/abug/timesfm` HEAD `9653264` master。

**Spec:** `docs/superpowers/reports/2026-09-11-audit/SUMMARY.md` 「只改文档」D1–D9 + `spec-alignment/SUMMARY.md` 失效横幅。

## Global Constraints

- 活仓 `/home/abug/timesfm`。禁止改：`config/prediction_scheme.py` 逻辑、`cascade/daily_model.py`、`cascade/hourly_model.py`、`cascade/features.py`、`data/config.py`、`evaluator.gate`、`BacktestDataStore` 行为、`.env*`、`aligned_verdicts.jsonl`、`supervisor_state.json`、生成物 `task_FM/*.inc.md`。
- 允许：`docs/**/*.md`、根 `AGENTS.md`/`CLAUDE.md`/`STATE.md`、`*/AGENTS.md`、`cascade/signal_contract.py` **模块 docstring**、`config/backtest_config.py` **注释**、`scripts/praxist_goal.yaml` **注释**。
- 权威数字（2026-09-11 磁盘）：
  - `STEP=2`，`EVAL_WINDOW_BARS=1200`，理论 n=589；verdict n 分布 396×21 / 588×3 / 324 / 142。
  - `gate_pass` = n≥350 且 ic=2×|dir_acc−0.5|≥0.05，**不含 EV**。
  - 经济过门 `pass_variants()` 要 ev>0：实质仅 `ss_vor`（n=396, ic=0.06, ev=+11.06）。
  - `gate_pass=True` 且 ev<0：`i_oi` (−2.46)、`m_ccl` (−3.64)。
  - `cj_oi` n=324 ic=0.08 ev=+19.46 gate=false（欠样本）。
  - SCHEMES SS = `calendar_cyclical`；`ss_vor` 未固化。
  - supervisor_state：`phase=fast`，`cycles_done=1`，`paused_429=false`，`last_run_id=run_2026-09-10_03-06-50_primary_task_FM`，`last_harvested_run_id=run_2026-09-10_00-44-51_primary_task_FM`。
  - goal.yaml 预算已是 999999 / deadline 2099-12-31；成功条件仍是 1 星集合 ≥4。
  - 可交易方向：`cascade_predict` / `monthly_backtest` = `position_from_forecast`；**Copilot 卡面仍是 `_compute_direction_v2`（日线）**。
  - `get_safe_daily` 只接到 cascade 报告图，未接 `DailyModel.predict`。
  - 429 期间代码仍 harvest；failover 模型 id 不同可开新 run。
  - 宿主 8 vCPU / 7.7 GiB；swap 以 `free -h` 为准（勿写死 0）。
- 禁止实现 diagnostic 收割、LGBM 生产、Phase 15 固化、15GiB 作战、给 gate 加 EV。
- 历史计划不要把全部 `- [ ]` 改成 `- [x]`（太大且不准确）；文首加「已落地 / 勿再施工」即可。hardening/compile-skip spec 同理。
- 中文正文。不 push。

---

### Task 1: `docs/system_design.md`（D1）

**Files:** Modify `docs/system_design.md`

- [ ] 文首加合同声明：可交易方向以 `docs/product_positioning.md` CF-01 A 为准；IC/硬门以 `loop-constraints.md` 为准；本文若冲突以后者为准。版本改 1.1 (2026-09-11)。定位改「方向性建议」不是自动开平仓。
- [ ] 架构图「斜率/阈值」改为「加权 1H 方向（级联/回测）；Copilot 卡面暂用日线副标签」。
- [ ] §3.3：日线斜率是 **regime 副标签**，不是可交易方向。
- [ ] §4.2/§4.3/§10：SS 生产 SCHEMES=`calendar_cyclical`；慢环过门 `ss_vor` 未固化。拆开写。
- [ ] §5：删幽灵 `_compute_direction`。写 `position_from_forecast` 为级联/回测主方向；`_compute_direction_v2` 为日线副标签（R² 门）；**如实写 Copilot 目前把 v2 当卡面主方向（已知分叉，代码未改）**。`horizon_slope` 存储分数/天，展示 ×100。
- [ ] §7.1 IC 改为 `2×|dir_acc−0.5|`。§7.3 `gate_pass` 只 n+ic；另写 `econ_pass = gate_pass and ev>0`。点名 `i_oi`/`m_ccl`。
- [ ] §8 模板：【可交易方向】加权 1H；【日线状态】斜率。注明 Copilot 报告目前仍印日线方向。
- [ ] §9 激活路径改 `/home/abug/timesfm` + `.praxist-venv`。
- [ ] 防穿越：回测 `hour>=15`；实盘 `DailyModel.predict` 仍裸读（已知债）。
- [ ] STEP 写 2，不要 24。附录补 `cascade/signal_contract.py`。

---

### Task 2: Praxist 人类文档 + 覆盖链

**Files:**
- Modify: `docs/praxist.md`, `docs/runbook_praxist_three_loop.md`, `docs/superpowers/specs/2026-09-02-praxist-three-loop-design.md`, `docs/superpowers/plans/2026-09-02-praxist-three-loop.md`（只改文首+绑定解释第 4 条，不要重写 80k 任务体）, `docs/superpowers/specs/praxist_control_plane.md`, `docs/praxist_peer_evaluation_fix.md`, `scripts/praxist_goal.yaml` 注释, `docs/spec_hypothesis_driven_fast_loop_20260908.md`（若仍写「仅本 run glob」当现行）

- [ ] praxist.md §4 现场改「以 JSON 为准」+ 2026-09-11 磁盘快照（phase=fast, cycles_done=1, 预算无限, 过门 ss_vor, 亏钱过门 i_oi+m_ccl）。§6：冲突时以方案 A / loop-constraints / harvest_proposals 为准；**09-02 计划绑定解释第 4 条作废**。
- [ ] runbook：删「paused_429 期间不 harvest」。改为 429 期间仍 harvest_proposals（提案是本地文件）。harvest 节「活 run 结束且非 paused_429」改成与代码一致。failover：同提供商 resume 同一 run；model id 不同允许新 run_dir。
- [ ] 09-02 spec 文首：部分取代；peer diagnostic 已废；骨架（慢环写 verdict、daily 缓存、429 resume 同提供商）仍有效。
- [ ] 09-02 计划文首：历史施工单，已落地，勿再执行。绑定解释 #4 改成 harvest_proposals / proposals/*.json，并标明旧 diagnostic 句作废。
- [ ] 控制面：宿主改 7.7GiB；peer eval 作战改为「任何 TimesFM 现仅慢环」；flock=1 / 2.5GiB 仍有效。不要改代码去贴 15GiB。
- [ ] peer-eval-fix：状态改为失效（方案 A），禁止按本文给 peer 加评估。
- [ ] goal.yaml 头注释改 7.7GiB / 方案 A / 预算已无限制。

---

### Task 3: STATE / AGENTS / 索引 / 诚实分叉

**Files:**
- Modify: `STATE.md`（Praxist 快照节 + 死链 `/root/timesFM_fu` + 最后更新日期）, `docs/README.md`, `docs/AGENTS.md`（EV 单位）, `AGENTS.md`（timesFM_fu 行改为「其他仓库，勿共用权重」）, `config/AGENTS.md`（STEP=2；删「实盘≠回测方向」，改为级联/回测已统一加权 1H，Copilot 卡面仍日线）, `data/AGENTS.md`（cutoff 为 bar datetime + hour>=15 含当日日线；旧「仅日期」段标已修）, `cascade/AGENTS.md`（lgbm 归档）, `cascade/signal_contract.py` docstring（Production cascade_predict/monthly MUST；**copilot 目前未调用**）, `config/backtest_config.py` 注释 600→理论 589, `docs/copilot.md`（卡面方向=日线 v2，与 CF-01 A 分叉）, `docs/host_environment_assessment.md` 顶表 swap/分支名若过期则改, `loop-constraints.md` 补 failover 例外一句, `docs/audit_system_efficiency_20260908.md` 把已落地的 compile/连接泄漏标已落地

- [ ] STATE Praxist 节用磁盘事实，并写「过期时以 JSON 为准」。补 m_ccl。n：396=旧网格或 max_points 截断，588≈现行 589，600=goal 上限，硬门阈值 350。
- [ ] 死链改相对路径 `docs/superpowers/reports/...` 或标明历史。
- [ ] 不要重写整个 STATE 的 Phase 11 史诗。

---

### Task 4: spec/plan/research 横幅

**Files:**
- Modify: hardening spec+plan 文首「已落地 master `243693d`；接口仍约束；勿当待开工」。勾选框保持，加一句「空框不表示未实现」。
- Modify: compile-skip spec+plan 「已落地；状态待审阅过期」。
- Modify: `docs/research/slow_loop_evaluation_points_research.md` Historical；被 `ee1f176` + SPEC-003/`get_safe_daily` 部分取代；STEP 已是 2；回测无条件含当日已修；实盘 predict 仍裸读。删「必须立即修复」祈使句。
- Modify: 四份 A2 spec + `docs/superpowers/plans/2026-08-05-alpha2-next-steps.md` 文首 Track B 关闭、生产入口 0 import。
- Modify: Phase 15 spec 固化目标失效、函数留池。
- Modify: 07-28/07-29/08-04 covariate specs 文首「历史战役；活 SCHEMES 以 prediction_scheme.py 为准」。

- [ ] 每份只加/改文首横幅 + 一两句取代指针，不重写正文公式（正文当历史）。

---

### Task 5: 自检

**Files:** 无新文件。跑 grep 确认：活人类文档不再把 IC 写成 corr、不再把 STEP=24 写成当前、不再把绑定解释 #4 当现行、不再把 peer-eval-fix 标待执行、不再把 09-09 SIGTERM 快照写成「现在」。

- [ ] grep `corr(预测` `STEP=24` `paused_429`期间不 harvest` `状态: 待执行` in peer-eval-fix `cycles_done=6` as current.
- [ ] 写短报告 `docs/superpowers/reports/2026-09-11-audit/doc-sync-log.md`：改了哪些文件、故意没改哪些（生成物、历史 run 报告、.txt 副本）。

---

## 故意不改

- `task_FM/experiments/**`、`task_FM/docs/praxist_reports/**` 历史 run 产物
- 同名 `.txt` spec 副本（非合同）
- `known_verdicts.inc.md` / `covariate_menu.inc.md`（监督环生成）
- 代码缺口（CC-1、Copilot 方向、schema 校验）— 只在文档里标明
