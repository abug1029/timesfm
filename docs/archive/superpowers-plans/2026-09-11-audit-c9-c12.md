# 审核代码债 C9–C12

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. After each task: commit + code review. Fix Critical/Important before next task.

**Goal:** 落地审核剩余四项。C10 选「文档承认未后复权」，不删 `raw_close`、不改价位制度。

**Architecture:** 继续分支 `feat/audit-code-fixes`（已推 origin）。活仓 WSL `/home/abug/timesfm`。

**Tech Stack:** Python 3.11 `.praxist-venv`，pytest。

**Spec:** `docs/superpowers/reports/2026-09-11-audit/SUMMARY.md` C9–C12。

## Global Constraints

- `wsl -d Ubuntu-22.04`，根 `/home/abug/timesfm`。
- TDD。每任务复审。不 push 除非本任务结束用户已要求（用户已说提交推送：每个完成且复审通过的任务可以 `git push`）。
- 禁止改 SCHEMES 值、`evaluator.gate`、STEP、接回 diagnostic 收割。
- C9/C12 会改 `cascade/hourly_model.py` / `cascade/features.py`：用户已要求做剩余 task，视为确认。
- C11 改 `data/config.py`：同样已确认。只加路径解析，不改 TimesFM 公式。
- 不加载真 TimesFM 跑单测。

---

### Task 1: C9 ALIGN 1H 与协变量同一根序列

**Files:**
- Modify: `cascade/hourly_model.py`, `cascade/features.py`（`build_covariate_matrix` / `build_combo_covariate_matrix` 增加可选 `df_1h`）
- Create: `tests/test_hourly_align_frame.py`

**Interfaces:** HourlyModel 已读的 `df_1h` 传入 features，features 不再自己 `get_main_contract_1h`（传入时）。

- [ ] 失败测试：store 的 `get_main_contract_1h` 与 `get_klines_1h` 返回不同 close；传入 ALIGN 的 df 后协变量长度/末价跟传入帧走，不跟 MAIN。
- [ ] `build_*` 增加 `df_1h=None`；None 时保持旧行为。
- [ ] `HourlyModel.predict` 把已读 `df_1h` 传入。
- [ ] 可选：脚本扫描生产 db 哪些品种没有 `{SYM}_MAIN`，结果写入报告注释，不改采集。
- [ ] commit `fix(cascade): covariates use HourlyModel ALIGN 1H frame`
- [ ] 复审

---

### Task 2: C10 承认未后复权（不改价）

**Files:** `data/data_store.py` 注释（raw_close 短路）、`data/AGENTS.md`、`docs/system_design.md` 数据节。禁止删 `raw_close` 列。

- [ ] 文档写清：日线/1H 当前都未做截面后复权；`raw_close` 存在则跳过。
- [ ] 测试或注释锁住「有 raw_close 则不乘 adj」。
- [ ] commit `docs(data): admit no roll backward-adjust on live reads`
- [ ] 复审

---

### Task 3: C11 `get_timesfm_model_path`

**Files:** `data/config.py` 新增函数；`cascade/daily_model.py` / `hourly_model.py` `from_pretrained` 改走该路径。测试 mock 环境变量。

- [ ] 解析顺序：`FM_TIMESFM_MODEL_PATH` / `TIMESFM_MODEL_PATH` / `TIMESFM_WEIGHTS_DIR` / 本地 `models/timesfm-2.5-200m-pytorch`（若存在且非空）/ 最后回退 hub id。
- [ ] 若路径是 symlink，测试或代码 warning：隔离应复制不是 symlink（检测 is_symlink 打日志，不在本任务复制几十 GB）。
- [ ] 无权重目录时仍可用 hub id，避免拆掉开发机。
- [ ] commit `fix(config): resolve TimesFM weights via get_timesfm_model_path`
- [ ] 复审

---

### Task 4: C12 统一 `rsi_state` 构造

**Files:** `cascade/features.py` combo 路径；测试。

- [ ] 单路径 `rsi_state` 用日线 RSI 映射到 1H；combo 路径现在用 1H 收盘 RSI。统一为**同一函数**（日线映射，与单路径一致），避免同一名字两种量。
- [ ] 测试：combo 与单路径对同一输入 `rsi_state` 列相等。
- [ ] 不改 SCHEMES 列表。
- [ ] commit `fix(features): unify rsi_state in combo vs single path`
- [ ] 复审

---

C9 先于 C12（都动 features）。C10 与 C11 可在 C9 复审后并行否？SDD 禁止并行 implementer。顺序：C9 → C10 → C11 → C12。
