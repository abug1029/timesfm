# Phase 11 补遗 — M 补跑 + P/SR 基线 + JM 修正

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补全 Phase 11 数据空白 (M), 完成剩余可固化品种决策 (P/SR), 修正 JM 数据错误。

**Architecture:** 复用 Phase 11 已有的批次脚本框架 (`_batch_lib.sh` + `batch_run_one`)。M 补跑 7 个单协变量测试; P/SR 各跑 1 次基线对比。所有结果写入 JSONL + Registry。

**Tech Stack:** Python + TimesFM 2.5 (CPU-only) + monthly_backtest.py + _batch_lib.sh

**Spec:** Phase 11 单协变量穷举 (7 covariates × 20 varieties) 漏测 M, P/SR 有 GREEN 但未做基线对比, JM Registry 数据与 JSONL 不一致。

## Global Constraints

- `config/prediction_scheme.py` 是高风险路径, 修改后必须验证 import 成功
- 所有回测使用 `monthly_backtest.py` 完整 walk-forward (396pt), 禁止 3pt/7pt scan
- 回测前必须获得用户批准
- 使用 nohup 启动, 不用 run_in_background
- `PYTHONIOENCODING=utf-8` 必须设置 (Windows GBK 崩溃防护)
- 含 `\Pu Chong\` 的路径用 `\PUCHON~1\` 替代

---

### Task 1: JM Registry 修正

**Files:**
- Modify: `docs/backtest_registry.md` (JM 段 Phase 11 行)
- Modify: `memory/phase11-solidification-20260820.md`

**背景:**
Registry 声称 JM "2 GREEN (hs=1.06/rsi=1.09), baseline FAIL → 固化替换 rsi_state", 但 JSONL 实际数据:
```
jm: 7 tests, 0 GREEN
  hourly_slope  PF=0.90 EV=-0.050
  rsi_state     PF=0.85 EV=-0.079
```
JM 的 scheme 已被改为 rsi_state, 但依据数据有误。需修正 Registry 记录, 保留 scheme 不变 (rsi_state 是 JM 7 个协变量中 PF 最高的 0.85, 虽非 GREEN 但相对最优)。

- [ ] **Step 1: 修正 Registry JM Phase 11 条目**

将:
```
| Phase 11 单协变量穷举 (7 cov) | 2026-08-20 | Phase 11 | 396 | 50% | 2 GREEN (hs=1.06/rsi=1.09), baseline FAIL → 固化替换 rsi_state (审核修正: hs→rsi) |
```
改为:
```
| Phase 11 单协变量穷举 (7 cov) | 2026-08-20 | Phase 11 | 396 | 47% | 0 GREEN (best rsi=0.85), 弱信号品种, rsi_state 为相对最优 (保留) |
```

同时删除 JM 段中的 `| baseline (rsi_state+oi) |` 行 (该基线实际是 JD 的, 误放入 JM)。

- [ ] **Step 2: 修正记忆文件**

更新 `memory/phase11-solidification-20260820.md`, 将 JM 从 "审核修正" 移到 "数据存疑" 注释。

- [ ] **Step 3: 提交**

```bash
git add docs/backtest_registry.md && git commit -m "fix: JM Registry 修正 — JSONL 实际 0 GREEN (best PF=0.85), 非此前声称 2 GREEN"
```

---

### Task 2: M 补跑 Phase 11 (7 tests)

**Files:**
- Create: `scripts/batch_m_single_cov.sh`
- Create: `reports/data_ops/batch_m_progress.jsonl` (运行时生成)
- Modify: `docs/backtest_registry.md` (M 段追加 Phase 11 行)

**背景:**
M (豆粕) 在 Phase 11 完全缺失 — 不在任何批次脚本中。当前方案 `ha_body+calendar_cyclical` (2星, DirAcc=58.1%), 是系统中表现最好的品种之一。需要确定是否有更优单协变量。

**7 个协变量:** calendar_cyclical, rsi_state, oi, reversal_shadow, hourly_slope, ha_body, ao_accel

- [ ] **Step 1: 创建批次脚本**

创建 `scripts/batch_m_single_cov.sh`:
```bash
#!/bin/bash
set -euo pipefail
export PYTHONIOENCODING=utf-8
export PYTHONWARNINGS=ignore

cd D:/FlyBuddy/FM_a
source D:/FlyBuddy/timesfm/.praxist-venv/Scripts/activate

FM_ROOT="D:/FlyBuddy/FM_a"
JSONL="reports/data_ops/batch_m_progress.jsonl"
LOG="reports/data_ops/batch_m.log"

source scripts/_batch_lib.sh

: > "$LOG"
batch_init "batch_m"

echo "=== batch_m START $(date -Iseconds) ===" >> "$LOG"

for combo in calendar_cyclical rsi_state oi reversal_shadow hourly_slope ha_body ao_accel; do
    batch_run_one "m" "$combo" "M-${combo:0:3}" "$JSONL" "$LOG"
done

echo "=== batch_m DONE $(date -Iseconds) ===" | tee -a "$LOG"
```

- [ ] **Step 2: 验证脚本语法**

```bash
bash -n scripts/batch_m_single_cov.sh && echo "syntax OK"
```

- [ ] **Step 3: 启动回测 (需用户批准)**

```bash
nohup bash scripts/batch_m_single_cov.sh > reports/data_ops/batch_m_nohup.log 2>&1 &
```

预计耗时: ~3h (7 × 25min)

- [ ] **Step 4: 设置监控等待完成**

```bash
until grep -q "batch_m DONE" reports/data_ops/batch_m.log; do sleep 30; done
```

- [ ] **Step 5: 分析结果, 更新 Registry**

从 JSONL 提取 PF/EV/MaxDD, 判断 GREEN 数量, 追加到 Registry M 段末尾:
```
| Phase 11 单协变量穷举 (7 cov) | 2026-08-20 | Phase 11 | <n> | <da>% | <N> GREEN (<details>), <结论> |
```

- [ ] **Step 6: 提交**

```bash
git add docs/backtest_registry.md reports/data_ops/batch_m_progress.jsonl scripts/batch_m_single_cov.sh
git commit -m "feat: M 补跑 Phase 11 (7 tests) + Registry 补录"
```

---

### Task 3: P 基线对比

**Files:**
- Create: `scripts/batch_baseline_p_sr.sh`
- Create: `reports/data_ops/batch_baseline_p_sr_progress.jsonl`

**背景:**
P (棕榈油) 当前方案 `rsi_state+reversal_shadow` (1星), Phase 11 显示 `calendar_cyclical` PF=1.02 GREEN。需要看 baseline 是否 FAIL。

- [ ] **Step 1: 创建基线对比脚本**

创建 `scripts/batch_baseline_p_sr.sh`:
```bash
#!/bin/bash
set -euo pipefail
export PYTHONIOENCODING=utf-8
export PYTHONWARNINGS=ignore

cd D:/FlyBuddy/FM_a
source D:/FlyBuddy/timesfm/.praxist-venv/Scripts/activate

FM_ROOT="D:/FlyBuddy/FM_a"
JSONL="reports/data_ops/batch_baseline_p_sr_progress.jsonl"
LOG="reports/data_ops/batch_baseline_p_sr.log"

source scripts/_batch_lib.sh

: > "$LOG"
batch_init "batch_baseline_p_sr"

echo "=== batch_baseline_p_sr START $(date -Iseconds) ===" >> "$LOG"

run_baseline() {
    local sym="$1" label="$2"
    if [ -f "$JSONL" ] && grep -q "\"label\": \"$label\"" "$JSONL" 2>/dev/null; then
        echo "[SKIP] $label already done" | tee -a "$LOG"
        return 0
    fi
    echo "[START] $label: $sym (baseline)" | tee -a "$LOG"
    if python scripts/monthly_backtest.py "$sym" >> "$LOG" 2>&1; then
        local pf ev maxdd diracc
        pf=$(grep -aoE 'PF=[0-9.]+' "$LOG" | tail -1 || echo "PF=unknown")
        ev=$(grep -aoE 'EV_ratio=[+-]?[0-9.]+' "$LOG" | tail -1 || echo "EV_ratio=unknown")
        maxdd=$(grep -aoE 'MaxDD=[+-]?[0-9.]+%' "$LOG" | tail -1 || echo "MaxDD=unknown")
        diracc=$(grep -aoE '[0-9]+pts DirAcc=[0-9.]+' "$LOG" | tail -1 || echo "unknown")
        local metric="${diracc} ${pf} ${ev} ${maxdd}"
        echo "[DONE] $label: $metric" | tee -a "$LOG"
        echo "{\"label\": \"$label\", \"symbol\": \"$sym\", \"combo\": \"baseline\", \"pf\": \"$pf\", \"ev\": \"$ev\", \"maxdd\": \"$maxdd\", \"diracc\": \"$diracc\", \"ts\": \"$(date -Iseconds)\"}" >> "$JSONL"
    else
        echo "[FAIL] $label exit=$?" | tee -a "$LOG"
    fi
    sleep 8
}

run_baseline "p" "BL-p"
run_baseline "sr" "BL-sr"

echo "=== batch_baseline_p_sr DONE $(date -Iseconds) ===" | tee -a "$LOG"
```

- [ ] **Step 2: 验证并启动 (需用户批准)**

```bash
bash -n scripts/batch_baseline_p_sr.sh && echo "syntax OK"
nohup bash scripts/batch_baseline_p_sr.sh > reports/data_ops/batch_baseline_p_sr_nohup.log 2>&1 &
```

预计耗时: ~1h (2 × 30min)

- [ ] **Step 3: 分析结果**

对比 baseline PF vs Phase 11 GREEN PF:
- P: baseline (rsi_state+reversal_shadow) vs calendar_cyclical PF=1.02
- SR: baseline (rsi_state+oi+calendar_cyclical) vs reversal_shadow PF=1.03

若 baseline FAIL → 替换为 Phase 11 最佳 GREEN 协变量。

- [ ] **Step 4: 如达标, 修改 prediction_scheme.py**

P 替换 (若 baseline FAIL):
```python
covariate_type="calendar_cyclical",
covariate_types=["calendar_cyclical"],
```

SR 替换 (若 baseline FAIL):
```python
covariate_type="reversal_shadow",
covariate_types=["reversal_shadow"],
```

- [ ] **Step 5: 验证 import + 提交**

```bash
PYTHONIOENCODING=utf-8 python -c "from config.prediction_scheme import SCHEMES; print(SCHEMES['p'].covariate_type, SCHEMES['sr'].covariate_type)"
git add config/prediction_scheme.py docs/backtest_registry.md
git commit -m "feat: P/SR 基线对比固化 (待填写实际结果)"
```

---

### Task 4: 记忆同步

**Files:**
- Modify: `memory/phase11-single-cov-exhaustive.md`
- Modify: `memory/phase11-solidification-20260820.md`
- Modify: `memory/MEMORY.md`

- [ ] **Step 1: 更新 Phase 11 穷举记忆**

补录 M 数据, 修正 JM GREEN 数量 (2→0), 更新总计。

- [ ] **Step 2: 更新固化结果记忆**

追加 P/SR 固化结果 (若 Task 3 达标), 标注 JM 数据存疑。

- [ ] **Step 3: 提交记忆**

记忆文件不入 git, 但确认 MEMORY.md 索引链接正确。
